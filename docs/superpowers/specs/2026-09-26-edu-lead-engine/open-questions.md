# Open Questions

Undecided items for the Edu Lead Engine. When one is answered, record it in `decisions.md` and remove it here.

## Next design sessions

- **C2 + C3 part C:** Bot Skill schema and action registry; `Lead Engine Settings` fields; template
  context contract; multi-topic reply composition; course advisor scoring dimensions and weights;
  decision log schema; learning-signal capture; summary note content; Playground behaviour; cost-guard
  limits.
- **C4 (routing):** rule D tie-breaks; load measure (open conversations vs. leads this week); central
  team behaviour out of hours; reassignment deadline.
- **C5 (visibility):** funnel stage definitions; Dashboard App hosting (served by the CRM bench?) and
  how it authenticates the viewing agent.

## Data details to pin in the C1.6 plan

- Demo consultant email domain (must be non-routable).
- Which courses each branch offers (drives the schedule generator).
- Robotics placement: inside *Tin học trẻ em* (current assumption) or its own group.
