# Decision Log

Binding record of every decision for the Edu Lead Engine. Newest at the bottom. A decision is changed
only by adding a new entry that supersedes it (never by editing the old one).

Status: **approved** = the user chose it · **assumed** = proposed, not explicitly confirmed yet.

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-001 | 2026-09-26 | Hybrid conversation model (option C): buttons collect certain facts (course, branch, phone); common questions answered directly by the bot; hard questions or hot leads handed to a consultant with an AI summary | Fast answers for FAQs without letting AI handle what it cannot | approved |
| D-002 | 2026-09-26 | Use **TypeSafe Jev only** as the AI (the user holds a Jev key); no generative LLM | User choice | approved |
| D-003 | 2026-09-26 | Jev understands and decides; **reply text is composed from CRM data + templates**, never generated | Jev has no text generation (docs.typesafe.ai: choice / score / noul only). Side benefit: the bot cannot invent fees or schedules | approved |
| D-004 | 2026-09-26 | Emphasis on the **[D] Data** layer: courses, branches, consultants, schedules, FAQs are CRM data editable in the UI, not code constants | Changing a fee or opening a branch must not need a deploy | approved |
| D-005 | 2026-09-26 | Start with a **large demo dataset** modelled on tinhocsaoviet.com; real data replaces it later | No real fee/schedule data available yet; the site publishes no fees | approved |
| D-006 | 2026-09-26 | Routing rule **D = branch + specialty + hotness**: prefer same-branch consultant with the lead's course-group specialty; hot leads go to a team lead; fall back to least-loaded same-branch consultant, then central team. Data split into fine-grained, extensible groups | User choice | approved |
| D-007 | 2026-09-26 | Visibility **C = both**: CRM dashboard for managers + Chatwoot Dashboard App beside each conversation for consultants, both reading one AI Decision Log | User choice | approved |
| D-008 | 2026-09-26 | Conversation engine **approach 1**: Jev-led slot filling with buttons as fallback (ask only for missing slots; tiered Jev questions group→course, area→branch). Without a Jev key or on Jev error, the bot degrades to buttons only | Natural for free text ("Robotics cho con ở Dĩ An" fills two slots at once); approach 2 (keep the rigid button flow) rejected as too rigid for ~45 courses | approved |
| D-009 | 2026-09-26 | One program spec used as a roadmap; layers follow the 5 splitting rules in spec §6.1; layers 1–10 get detailed design first, later layers get detail when reached | Detailing 39 layers now would go stale before they are built | approved |
| D-010 | 2026-09-26 | Structure: **10 clusters / 39 layers** as listed in spec §6.2 | User approved | approved |
| D-011 | 2026-09-26 | Ops (production, backups, monitoring) and multi-tenancy are referenced from `ROADMAP.md` Phases 1–2, not duplicated here | Single place per concern | approved |
| D-012 | 2026-09-26 | Add **B2B lead detection → B2B team** as layer 18 in C4 | Sao Việt claims 15,000+ partner enterprises; a company training 20 staff is a different sale from one learner | approved |
| D-013 | 2026-09-26 | Cluster 1 data design as in spec §7.1: branches on CRM Territory, courses on CRM Product (custom fields), new DocTypes Course Group / Consultant / Course Schedule / Course Promotion / FAQ Topic in `mmm_custom`; no edits to vendored `crm/` | Extend upstream first (ROADMAP principle 4) | approved |
| D-014 | 2026-09-26 | Lead stores branch in the standard `territory` field and courses in the standard `products` table (lead value = sum of fees, carried to Deal); `course_interest` stays as a readable summary; the hardcoded `branch` Select is retired when layer 7 switches writers | `branch` Select holds only 3 hardcoded branches; standard fields are what CRM reports and Deal conversion already use | approved |
| D-015 | 2026-09-26 | Consultants are a CRM DocType (source of truth), replacing Chatwoot agent `custom_attributes.branch`; Chatwoot keeps only the agent id | CRM is the only source of truth (ROADMAP principle 2) | approved |
| D-016 | 2026-09-26 | Answer templates use **Jinja** via `frappe.render_template`; only administrators edit them | Native to Frappe; supports loops (e.g. list the next 3 schedules) | approved |
| D-017 | 2026-09-26 | One main spec file (`2026-09-26-edu-lead-engine-design.md`) plus companion files in this folder (agent briefing README, this log, code map, glossary, open questions); agents consult them instead of conversation memory | Avoid context drift / hallucination across long design conversations | approved |
| D-018 | 2026-09-26 | Spec files are written in English (repo rule, `AGENTS.md`); Vietnamese bot copy stays Vietnamese; diagrams in Mermaid | Repo convention; Mermaid renders on GitHub and diffs as text | approved |
| D-019 | 2026-09-26 | **Extensibility is the top design priority**: anything a business may vary (slots, questions, options, FAQ topics, templates, routing, thresholds) is CRM data; code extension points are small registries (e.g. slot types, actions), so a new capability is a data row or one small handler — not an engine rewrite. Use the full feature set of Chatwoot and Frappe CRM before custom code. Goal: a complete product end users can operate | User: "điểm quan trọng nhất là phải dễ mở rộng và vận dụng hết khả năng của project" | approved |
| D-020 | 2026-09-26 | Bot collects: course, branch, phone (required) **plus** learner (self / child with age / company staff), preferred shift, customer name; slots defined as data so more can be added | User choice; richer data for routing (B2B, age-appropriate courses) and schedule matching | approved |
| D-021 | 2026-09-26 | Handoff **as soon as the required slots are filled**; while the customer waits for the consultant's first reply the bot still answers FAQs | User choice (recommended option) | approved |
| D-022 | 2026-09-26 | Returning customers are **recognised and continued**: greet by name, never re-ask known facts, add newly asked courses to the Lead, route back to the consultant who owns the Lead | User choice (recommended option) | approved |
| D-023 | 2026-09-26 | Slots are rows of a new **`Bot Slot`** DocType (`slot_key`, `label`, `slot_type`, `required`, `sort_order`, `ask_template`, `options` with aliases, `depends_on`, `lead_field`); slot types are a small code registry: `catalog`, `choice`, `phone`, `number`, `text`. Initial slots: course★, branch★, phone★, learner, learner_age (if child), preferred_shift, customer_name | D-019/D-020: a new question is a data row; a new kind of answer is one small handler | approved |
| D-024 | 2026-09-26 | Conversation state lives in a new **`Bot Conversation`** DocType, one per Chatwoot conversation (lead, status active/handed_off/closed, filled slots with source + confidence, pending confirmation, last processed message id); a returning customer's new conversation is prefilled from the Lead. Replaces contact `custom_attributes.bot_state` | Per-contact state silences the bot forever after one completion (bug in current `bot_api.py`); per-conversation state enables D-022 and is queryable for C5 | approved |
| D-025 | 2026-09-26 | Webhook verifies HMAC, enqueues a background job (`job_id` = message id, `deduplicate=True`) and returns immediately; the job holds a per-conversation lock; pipeline = load → understand → **decide (pure function, offline-testable)** → act | Chatwoot agent-bot webhooks time out after 5 s and retry up to 3 times → duplicate replies otherwise; Frappe v15 provides `enqueue(job_id, deduplicate)` and `filelock` | approved |
| D-026 | 2026-09-26 | After handoff the bot answers FAQs only until the consultant's first message, then stays silent in that conversation | Never talk over a human (refines D-021) | approved |
| D-027 | 2026-09-26 | Use Chatwoot natively: labels (course group, branch, hotness), branch Teams for assignment, conversation `custom_attributes` mirroring filled slots (visible in the Chatwoot sidebar before C5) | D-019: use platform features before custom UI | approved |
| D-028 | 2026-09-26 | Two-tier understanding: (1) deterministic alias/regex matcher over diacritic-folded text (catalog aliases, slot options, phone, numbers); (2) one Jev call with parallel questions **generated from data** (only for missing slots + FAQ topic + intent + hotness + wants-human noul); button taps skip Jev. State = latest message, ~10 recent turns, known slots, the bot's pending question. Criteria text in English with Vietnamese aliases. **Refines D-008**: tiers are asked in parallel and cross-checked (course must belong to group, branch to area) instead of sequentially | Jev accuracy is lower for Vietnamese (docs: Models); parallel questions add no latency; max 255 options per choice | approved |
| D-029 | 2026-09-26 | Combination rules: unique keyword match wins unless Jev confidently disagrees (→ confirm); ambiguous keyword → Jev picks among candidates or buttons limited to them; group/course or area/branch mismatch → drop the child, keep the parent if confident | Keyword precision + Jev for negation/ambiguity | approved |
| D-030 | 2026-09-26 | Three confidence bands (act / confirm / fall back) with per-decision thresholds stored in a new single DocType **`Lead Engine Settings`**; starting values: catalog slot 0.85/0.55, choice slot 0.80/0.50, FAQ answer 0.85/0.60, early handoff noul 0.70; confirmation = [Đúng ạ] [Không phải] turn stored as `pending_confirmation` | Jev docs: thresholds scale with risk; medium confidence → ask the user | approved |
| D-031 | 2026-09-26 | Jev timeout 8 s in the bot path; error/timeout → keyword tier only, logged `jev_unavailable` | Keep reply latency low; success criterion 5 | approved |
| D-032 | 2026-09-26 | While a Bot Conversation is active the bot's Jev call supplies intent/hotness and `intelligence.analyze_conversation` skips it; after handoff `intelligence.py` runs as today | No double token spend, no conflicting answers | approved |
| D-033 | 2026-09-26 | Layer 11 gate: ~100 labelled hard Vietnamese utterances in the repo + evaluation tool against real Jev; Jev goes live for real customers only with **0 wrong course/branch/FAQ answers in the act band**; keyword tier unit-tested offline | Measure before trusting | approved |
| D-034 | 2026-09-26 | Chatwoot sends a Messenger quick-reply tap back as the **button title** (`send_on_facebook_service.rb`: `payload: item['title']`); the bot stores the offered options (title → value) in `Bot Conversation.pending` and maps taps exactly; titles unique per turn, ≤20 chars | Replaces today's fuzzy title matching (commit 98ec763) | approved |
| D-035 | 2026-09-26 | **`Bot Skill` replaces `FAQ Topic`** (supersedes the FAQ Topic part of D-013): a skill = description Jev reads + parameters (slot keys) + action type from a small code registry (`answer_template`, `schedule_lookup`, `send_media`, `recommend_courses`, `book_appointment`, `handoff`) + templates + `creates_lead`. New bot capability = a data row; new action type = one small handler | Jev "function calling" pattern; D-019 | approved |
| D-036 | 2026-09-26 | The engine emits events (`slot_filled`, `skill_done`, `handed_off`, `lead_updated`, …) through a Frappe hook (`lead_engine_events` in `hooks.py`); later features subscribe instead of editing the engine | D-019; Frappe-native extension point | approved |
| D-037 | 2026-09-26 | Brand and voice are data in `Lead Engine Settings` (brand name, how the bot addresses customers, hotline, sign-off) and part of every template's context; no template hardcodes a business | Multi-SMB readiness (ROADMAP principle 7) | approved |
| D-038 | 2026-09-26 | **Playground** page in the CRM: type a message, see every step (keyword matches → Jev answers → decision → rendered reply) without Messenger; replay any logged decision. Last layer of C2 so C3 work is tested with it | Non-developers test skills/templates; doubles as the live demo of the AI | approved |
| D-039 | 2026-09-26 | Multi-topic messages: one `noul` per skill in the same Jev call (fan-out); the reply combines up to N answered skills (setting, default 3) | Jev "speculative fan-out"; parallel questions add no latency | approved |
| D-040 | 2026-09-26 | **Course advisor** skill: vague needs → filter courses by data (age range, audience, group) → Jev composite scoring of fit per shortlisted course → top 3 with fees; adds optional slots `goal` and `level` (Bot Slot rows) | Jev composite-scoring / re-ranking patterns; turns the bot into a consultant | approved |
| D-041 | 2026-09-26 | Course cards (image + buttons) on Messenger as its own layer in C5: vendored Chatwoot edit mapping content type `cards` to a Messenger generic template, recorded in `docs/vendored-upstreams.md`; until then the bot can send a course poster image (CRM Product `image`) as an attachment | Chatwoot's Facebook sender supports only text, quick replies and attachments today | approved |
| D-042 | 2026-09-26 | Date/time understanding for trial/consultation appointments ("chiều thứ 7 này"): Jev reads weekday/part-of-day as choices, code resolves the date; merged into the appointments layer in C6 | Jev date-extraction cookbook; model never does calendar math | approved |
| D-043 | 2026-09-26 | Learning signals are captured from C2 on (rejected confirmations, consultant corrections of course/branch on the Lead, unmatched terms before handoffs) and turn into evaluation examples and alias proposals; the admin review/approve UI comes in C9 | The bot improves without developers; data must be collected from day one | approved |
| D-044 | 2026-09-26 | Cost guard: per-conversation cap on Jev calls per hour (setting), skip Jev when the keyword tier resolves every open question, bot stops on confident spam | A single abuser must not burn tokens without limit | approved |
| D-045 | 2026-09-26 | A/B template variants per skill with conversion measurement, in C9 | Optimise wording on real outcomes | approved |
| D-046 | 2026-09-26 | Out-of-hours bot behaviour (say when a person will call back, still collect slots, create a next-morning task) merged into the working-hours layer in C4 | No lead forgotten overnight | approved |
| D-047 | 2026-09-26 | Layer IDs become cluster-local **`Cn.m`** (stable when layers are inserted); roadmap re-split to **46 layers** per the §6.1 rules; old numbers 1–39 map as listed in spec §6.3 | Inserting layers no longer renumbers everything | approved |
| D-048 | 2026-09-26 | `Bot Skill` fields: `skill_key`, `title`, `jev_description` (EN) + `examples` (VN, also eval seeds), `aliases` (keyword triggers), `parameters` (Bot Slots), `missing_policy` (`ask` = ask the slot then answer, remembered as `pending_skill`; `generic` = answer at group level), `action_type` + `action_config`, `templates` child table (variants with a Jinja `when` condition), `follow_ups` (≤20-char buttons → skill / slot / handoff), `media`, `creates_lead`, `handoff_after`, `sort_order`, `active` (refines D-035) | Data-defined skills with guided next steps | approved |
| D-049 | 2026-09-26 | Initial action registry: `answer_template`, `schedule_lookup`, `fee_quote` (code computes final fee with promotions), `branch_info`, `send_media`, `recommend_courses`, `handoff`; later layers add `book_appointment` (C6.3) and course cards (C5.5). All arithmetic and lookups are code, never Jev | D-003, D-019 | approved |
| D-050 | 2026-09-26 | Fixed template context contract (`brand`, `customer`, `course`, `branch`, `area`, `schedules`, `promotions`, `final_fee`, `recommendations`, `slots`, `missing`; filters `vnd`, `date_vi`) documented in spec §7.2 | Template authors know exactly what they can use | approved |
| D-051 | 2026-09-26 | Render guard: Frappe's Jinja is sandboxed but uses `DebugUndefined` (missing variables print literally), so any rendered reply still containing `{{` or `{%` is not sent; the settings fallback template is sent and a `render_error` is logged | Customers must never see template syntax | approved |
| D-052 | 2026-09-26 | `Lead Engine Settings` holds brand/voice (brand name, bot name, address forms, hotline, Zalo, website, greeting, fallback, sign-off), `max_skills_per_reply` (3), Part B thresholds, cost-guard limits, advisor weights and floor, handoff and summary templates | One place to tune the engine without code | approved |
| D-053 | 2026-09-26 | Add `button_label` (≤20 chars) to Course Group, CRM Product and CRM Territory (amends C1 custom fields) | Messenger quick-reply titles are limited to 20 chars; course names are longer | approved |
| D-054 | 2026-09-26 | Multi-skill reply: skills above threshold ordered by `sort_order`, capped at N; one paragraph each; the question for the next missing required slot is always appended; up to 3 follow-up buttons; split into two messages above Messenger's 2,000-char limit | Answer and advance in one turn | approved |
| D-055 | 2026-09-26 | Course advisor: code filters by learner/audience, age range, group → shortlist ≤8; Jev scores `goal_fit` and `level_fit` per course (+ exclusion noul); composite 0.6/0.4 (settings); top 3 with fees; below the floor → ask `goal`/`level` by buttons; at most one extra Jev call per turn | Jev composite scoring / re-ranking; bounded cost | approved |
| D-056 | 2026-09-26 | **`AI Decision Log`**: one row per processed customer message (context, keyword matches, Jev questions/answers/confidence, model version, latency, input tokens, Jev status, decision type, slots before/after, skills answered, a readable Vietnamese reason, reply + template variants, errors). Full read access only for System Manager / sales managers (consultants see their own conversations via C5); retention setting (default 180 days) enforced by a daily purge job | One record explains a whole turn; feeds dashboards, Playground replay and evaluation; personal data handled per Decree 13/2023 | approved |
| D-057 | 2026-09-26 | **`Bot Learning Signal`** types: `confirm_rejected`, `consultant_corrected` (CRM Lead `doc_events` compares `get_doc_before_save()` for `territory`/`products`; saves made by the engine carry `doc.flags.lead_engine` and are skipped), `unmatched_term`, `stuck`, `render_error`; status `new` → `accepted_as_example` / `alias_proposed` / `dismissed` | D-043 capture from C2 on | approved |
| D-058 | 2026-09-26 | Handoff triggers: required slots filled, wants-human, hot, skill `handoff_after`, stuck (`max_stuck_turns`, default 2). Steps: pick consultant (C2.6: Consultant in the lead's branch with fewest open conversations; returning customer → Lead owner; C4.1 replaces with full rule D) → Chatwoot assign agent + branch team, `open`, labels, attributes → Lead `lead_owner` → customer handoff message (settings template) → private summary note (settings template; the next-step suggestion is computed by code from data) → `handed_off` event → log | Clean, explained handoff | approved |
| D-059 | 2026-09-26 | The inbox Agent Bot receives every `message_created` (customer, agent, and the bot's own messages) plus status events: the bot ignores its own messages, sets `consultant_replied` on the first non-private message sent by a human agent, and closes the Bot Conversation when the conversation is resolved | Verified in `chatwoot/app/listeners/agent_bot_listener.rb`; implements D-026 | approved |
| D-060 | 2026-09-26 | Playground runs dry (never sends to Chatwoot or writes Leads) and can replay any decision-log row with current data/templates/thresholds, showing then-vs-now. All side effects go through an **`Effects`** interface: real in production, a recorder in Playground and tests. Playground Jev calls use a separate quota | Safe experimentation; testable engine | approved |
| D-061 | 2026-09-26 | Cost guard defaults: 20 Jev calls per conversation per hour; site-wide daily token budget (over budget → keyword-only + warning); skip Jev on button taps or when keywords fill every open slot and no content words remain; input caps 1,000 chars per message and 10 turns of state; spam intent ≥0.8 → label spam, close, stop, no Lead | Bounded spend per conversation and per day (refines D-044) | approved |
| D-062 | 2026-09-26 | C2–C3 verification: offline unit tests (decide, keyword matcher, slot types, render guard, reply composition, advisor math); integration script `scripts/test-bot-conversation.py` on a `-p crmverify` stack with signed webhooks (buttons-only flow to handoff, multi-slot free text, returning customer, duplicate delivery → one reply, Jev disabled); Playground screenshots for UI; C3.1 gate before Jev serves real customers | Evidence before claims | approved |
| D-063 | 2026-09-26 | Spec self-review refinements: `Bot Conversation` also has `pending_skill`, `stuck_turns`, `is_sandbox` (Playground sessions, excluded from dashboards); without Jev a unique skill-alias match acts, with Jev the skill's `noul` sets the band and an alias match lifts it to at least confirm; the webhook drops the bot's own messages and private notes before enqueueing; degraded mode = keyword tier + tiered buttons | Close gaps between parts A–D found in self-review | approved |
